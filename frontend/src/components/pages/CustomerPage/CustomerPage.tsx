import { type FC } from "react";
import { CustomerTemplate } from "@/components/template";

export const CustomerPage: FC = () => {
  return (
    // adicionar header
      <CustomerTemplate />
    // adicionar footer
  );
};
