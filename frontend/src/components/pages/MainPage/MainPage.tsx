import { type FC } from "react";
import { MainTemplate } from "@/components/template";

export const MainPage: FC = () => {
  return (
    <MainTemplate>
      <h1>main page</h1>
    </MainTemplate>
  );
};
