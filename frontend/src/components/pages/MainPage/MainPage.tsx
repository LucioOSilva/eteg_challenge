import { type FC } from "react";
import { MainTemplate } from "@/components/template";

export const MainPage: FC = () => {
  return (
    // adicionar header
    <MainTemplate>
      <p>content</p>
    </MainTemplate>
    // adicionar footer
  );
};
